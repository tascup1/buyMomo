{
  "name": "momo-sniper",
  "version": "1.0.0",
  "private": true,
  "description": "momo 定時搶購小幫手(Puppeteer,使用系統 Chrome)",
  "main": "auto-order.js",
  "bin": "auto-order.js",
  "scripts": {
    "start": "node auto-order.js",
    "build": "pkg . --compress GZip --no-bytecode --public --public-packages '*' && npm run zip",
    "zip": "cd dist && zip -X momo-sniper-mac-apple晶片.zip momo-sniper-macos-arm64 && zip -X momo-sniper-win.zip momo-sniper-win-x64.exe && rm -f momo-sniper-macos-arm64 momo-sniper-win-x64.exe"
  },
  "pkg": {
    "targets": [
      "node22-win-x64",
      "node22-macos-arm64"
    ],
    "outputPath": "dist"
  },
  "dependencies": {
    "puppeteer-core": "^23.0.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.21.0"
  }
}
